<li id="break-<%= bbreak.id %>">
  <span class="title">bbreak <%= bbreak.id %></span>
  <span class="points"><%= bbreak.points %> points</span>
  <span class="timestamp">
    Done in <%= time_ago_in_words(bbreak.created_at) %> ago.
  </span>
</li>
